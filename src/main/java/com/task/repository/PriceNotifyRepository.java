package com.task.repository;

import com.task.entity.PriceNotify;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PriceNotifyRepository extends JpaRepository<PriceNotify,Long> {
}
